interface:
  display_name: "Windows PowerShell Encoding"
  short_description: "Avoid UTF-8 and mojibake pitfalls on Windows with pwsh 7.6.1+"
  default_prompt: "Use $windows-powershell-encoding-skill to inspect this Windows PowerShell workflow for UTF-8, CJK, and mojibake risks. Use pwsh 7.6.1+ for validation scripts."

policy:
  allow_implicit_invocation: true
